# Server configuration
server {
    host 127.0.0.1;
    port 8080; # HTTP port
    port 8081;
    port 8082;

     client_max_body_size 10M;

     location / {
        root ./resources;
        methods GET POST;
        index index.html;
        directory_listing off;
     }
}
